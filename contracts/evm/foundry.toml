[profile.default]
src = "src"
out = "out"
libs = ["lib"]
solc = "0.8.24"
optimizer = true
optimizer_runs = 200

# Fail builds on solc warnings (Foundry expects `deny = "warnings"`).
deny = "warnings"
bytecode_hash = "none"
