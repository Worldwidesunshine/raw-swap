# Rewards & farming (Wave 18)

Targets:

- Solana: evaluate Quarry / custom staking for RAWSWAP emissions.
- EVM: MasterChef-style distributor keyed on LP token balances.
- Cross-chain claims: NTT bridge on claim if rewards move hub↔spoke.

DB: `rewards_epochs` table (see migration `0003_liquidity_and_fees.sql`).
