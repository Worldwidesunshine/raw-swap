# Hardening & launch (Wave 20)

- Security review checklist (signer custody, fee caps, router integrity).
- Rate limits tuned per route (Fastify plugins).
- Solana CU profiling on largest Jupiter routes with fee ixs appended.
- EVM gas snapshots on fee vault + pool actions.
- OpenAPI refresh + public docs site.
- Mainnet launch runbook (sequenced deploy: mint → pools → fee env → compounder).
